name: CI

on:
  push:
  pull_request:

jobs:
  macos-native:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Compile Swift
        run: swiftc -O teleprompter.swift -o teleprompter
      - name: Verify binary
        run: test -x teleprompter
